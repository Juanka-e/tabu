# Domain Game

Transport-independent game mode contracts and rules.

`apps/web` owns Socket.IO, room presence and UI payloads. This package owns mode
identity, settings normalization and deterministic match decisions.
